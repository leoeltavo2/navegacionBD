<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bdnavigation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/database"
        android:supportsRtl="true"
        android:theme="@style/Theme.BDNavigation">
        <activity android:name=".ui.ver.producto.ModificarProductoActivity"></activity>
        <activity android:name=".ui.ver.persona.ModificarPersonaActivity" />
         <activity android:name=".SplashActivity">
         <intent-filter>
         <action android:name="android.intent.action.MAIN" />
         <category android:name="android.intent.category.LAUNCHER" />
         </intent-filter>
         </activity>
        <activity android:name=".ui.ver.producto.VerProductoActivity" />
        <activity android:name=".ui.ver.persona.VerPersonaActivity" />
        <activity android:name=".ui.agregar.producto.AgregarProductoActivity" />
        <activity android:name=".ui.agregar.persona.AgregarPersonaActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.BDNavigation.NoActionBar">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
    </application>

</manifest>